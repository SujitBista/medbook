{
  "name": "medbook",
  "version": "0.1.0",
  "private": true,
  "description": "Doctor Appointment Booking App - Monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medbook_test TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medbook_test PGUSER=postgres PGPASSWORD=postgres PGDATABASE=medbook_test turbo run test",
    "test:watch": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medbook_test TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medbook_test PGUSER=postgres PGPASSWORD=postgres PGDATABASE=medbook_test turbo run test:watch",
    "test:coverage": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medbook_test TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medbook_test PGUSER=postgres PGPASSWORD=postgres PGDATABASE=medbook_test turbo run test:coverage",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "db:generate": "pnpm --filter @app/db db:generate",
    "db:migrate": "pnpm --filter @app/db db:migrate",
    "db:migrate:deploy": "pnpm --filter @app/db db:migrate:deploy",
    "db:migrate:create": "pnpm --filter @app/db db:migrate:create",
    "db:migrate:reset": "pnpm --filter @app/db db:migrate:reset",
    "db:seed": "pnpm --filter @app/db db:seed",
    "prepare": "node -e \"if(process.env.CI || !require('fs').existsSync('.git')) process.exit(0); require('child_process').exec('husky', (e) => e && process.exit(0))\""
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^4.0.14",
    "@vitest/coverage-v8": "^4.0.14",
    "@vitest/ui": "^4.0.14",
    "eslint": "^8.57.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.2.5",
    "turbo": "^2.0.0",
    "typescript": "^5.3.3",
    "vitest": "^4.0.14"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
