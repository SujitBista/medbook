# Node Environment
# Options: development, production, test
NODE_ENV=development

# Server Port
# Default: 4000
PORT=4000

# API URL
# Full URL where the API is accessible
# Default: http://localhost:{PORT}
API_URL=http://localhost:4000

# CORS Configuration
# Comma-separated list of allowed origins
# Origins are normalized (lowercase, no trailing slash)
# Default: http://localhost:3000,http://127.0.0.1:3000,http://[::1]:3000
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000,http://[::1]:3000

# Allow requests with no Origin header
# Set to 'true' to allow requests without Origin header (e.g., Postman, curl)
# Default: false
CORS_ALLOW_NO_ORIGIN=false

# Allow requests with "null" origin
# Set to 'true' to allow Origin: null (file:// protocol, sandboxed iframes)
# Default: false
CORS_ALLOW_NULL_ORIGIN=false
