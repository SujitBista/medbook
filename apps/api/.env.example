# API Server Configuration

# Node environment: development, production, or test
# Default: development
NODE_ENV=development

# Server port
# Default: 4000
PORT=4000

# Full URL where the API is accessible
# Default: http://localhost:{PORT}
API_URL=http://localhost:4000

# CORS Configuration
# Comma-separated list of allowed origins (automatically normalized)
# Default: http://localhost:3000,http://127.0.0.1:3000,http://[::1]:3000
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000,http://[::1]:3000

# Allow requests with no Origin header (e.g., Postman, curl, server-to-server)
# ⚠️ Security Warning: Only enable in development or for trusted server-to-server communication
# Default: false
CORS_ALLOW_NO_ORIGIN=false

# Allow requests with Origin: null (file:// protocol, sandboxed iframes)
# ⚠️ Security Warning: Only enable if you specifically need to support file:// protocol
# Default: false
CORS_ALLOW_NULL_ORIGIN=false
