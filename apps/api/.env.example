# Server Configuration
NODE_ENV=development
PORT=4000
API_URL=http://localhost:4000

# CORS Configuration
# Comma-separated list of allowed origins (normalized: lowercase, no trailing slash)
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000,http://[::1]:3000
# Allow requests with no Origin header (default: false)
CORS_ALLOW_NO_ORIGIN=false
# Allow requests with "null" origin, e.g., from file:// protocol (default: false)
CORS_ALLOW_NULL_ORIGIN=false

# Database (will be added in Phase 1.4)
# DATABASE_URL=postgresql://user:password@localhost:5432/medbook

# Authentication (will be added in Phase 2)
# JWT_SECRET=your-jwt-secret-here
# NEXTAUTH_SECRET=your-nextauth-secret-here
